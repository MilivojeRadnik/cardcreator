extends layout 

block content 
  
  .forma
    form(action='' method='post' )
      input#name(type='text' name='name' required='true' placeholder='Ime karte')
      label(for='weight') Tezina:
      input#weight(type='number' name='weight' required='true' min='0' max='5' value='0')
      .img
        label#preview( style='cursor: pointer;' disabled="true") IZABERI SLIKU
      textarea#description(rows='3' name='description' required='true' placeholder='Opis - kad kako sta')
      input(type='submit' value='POSALJI')
  a(href='/list-select' style='margin: 10px;') Izlistaj karte
  script.
    var loadFile = function(event) {
    var image = document.getElementById('output');
    var preview = document.getElementById('preview');
    preview.innerHTML = `<img id='output' src='${URL.createObjectURL(event.target.files[0])}' />`
    };
